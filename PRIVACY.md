# 隐私说明

适用版本：v0.1.1 起。更新日期：2026-09-13。

Codex Usage Monitor 由 GitHub 用户 sujiangbai 维护，是独立的开源小工具，非 OpenAI 官方产品。项目没有接收使用数据的服务器，也不内置遥测、广告或崩溃上报。一般问题可在项目仓库联系维护者；涉及账号、漏洞或隐私的问题请使用 [私密漏洞报告](https://github.com/sujiangbai/Codex-Usage-Monitor--macOS/security/advisories/new)，不要公开个人信息。

## 开始前的选择

首次使用或从 v0.1.0 升级后，组件在你点击「开始监控」之前不启动额度查询进程。关闭面板或选择登录启动不会代替这一选择。请仅监控你有权使用的本机登录账号；共用同一个 macOS 用户会话时，组件无法识别屏幕前是否为账号持有人。

## 数据流与用途

组件 → 本机 Codex app-server → 官方服务 → 本机组件显示。

- 组件通过标准输入输出调用 `initialize`、`initialized`、`account/rateLimits/read`，用于显示当前登录账号的剩余额度与重置时间。
- 仅保留显示所需的额度比例、周期长度、重置时间、订阅类型和额度分组标识。额外响应字段可能经过内存中的解析，但不用于展示或持久保存；不会保存原始响应。
- 组件不直接读取或复制登录凭据，不读取聊天记录、项目文件、浏览器 Cookie，不请求模型推理、发送消息、购买余额或消耗重置券。
- 查询响应仅在运行内存中使用；新查询替换旧结果，停止监控会丢弃保留的额度，退出会结束进程。这里不承诺操作系统交换区或崩溃机制中的字节级安全擦除。
- 不向本项目维护者上传额度或账号数据。官方 Codex 使用已有认证联网，组件不是离线工具；官方服务对网络请求及账号信息的处理受其自身条款和隐私政策约束。

## 本机 Codex 与设置

组件寻找已安装的 Codex 应用或常见 CLI 路径。当前版本不对候选文件执行额外的发布者签名验证，请从可信来源安装 Codex。启动的进程会继承运行环境，并可能根据 Codex 的版本、本机配置、代理和认证设置产生它自己的网络访问及状态文件；本项目不能保证第三方进程完全没有日志或其他状态变化。

组件在进程参数中关闭 Codex 的 analytics 和 OpenTelemetry 日志、追踪、指标导出，丢弃标准错误，不改写 Codex 配置。上述控制不等于禁止官方服务维护服务端运行记录。

组件通过 macOS UserDefaults 保存以下三项偏好，直到你删除它们：

| 键 | 用途 |
| --- | --- |
| `displayPeriod` | 菜单栏显示每周或 5 小时额度 |
| `startupChoiceMade` | 是否已选择登录启动选项 |
| `monitoringConsentV1` | 是否允许开始监控 |

偏好域为 `local.codexquota.menubar`，不包含账号标识、Token 或额度历史。登录项状态由 macOS 单独管理，只有你点击开启时才注册。

## 停止、撤回与删除

点击面板中的「停止监控」即可取消正在进行的查询、停止后台刷新、丢弃保留的额度，并保存不允许监控的状态。重新启动也不会自动恢复查询；需要再次点击「开始监控」。停止监控不更改登录启动设置，也不退出或注销官方 Codex。

彻底停用时，先关闭「登录 Mac 时启动」，再退出并删除应用。若已停止监控，可在 macOS 系统设置的「登录项」中关闭它。退出后可在终端运行以下命令删除本组件偏好（不会删除 Codex 账号或聊天）：

```sh
defaults delete local.codexquota.menubar
```

没有偏好时该命令可能提示域不存在。重新安装后的首次查询仍需重新选择。由于本项目不接收用户额度或账号记录，维护者没有此类服务器记录可导出或删除；官方服务中的记录需要通过其对应渠道管理。

## 公开反馈与屏幕显示

菜单栏百分比和面板数据可能被同屏人员、截图或屏幕共享看到。反馈时请使用示例模式或手动遮盖真实数据，不要上传 `auth.json`、Token、Cookie、原始响应、个人路径或完整配置。GitHub 托管的下载、Issue 和私密报告由 GitHub 按其规则处理，不等同于本组件的本地查询。

未来若增加数据上传或改变用途，会更新说明并在需要时重新请求选择。本文件描述产品行为，不构成对所有地区法律适用性的保证。
